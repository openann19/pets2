/**
 * Absolute minimal test to diagnose environment issues
 */

// No imports at all

describe('Minimal Test Suite', () => {
  it('should pass a simple test', () => {
    expect(1 + 1).toBe(2);
  });
});
