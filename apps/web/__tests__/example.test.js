/**
 * Example test to help determine the right structure
 */
import { expect } from '@jest/globals';

describe('Basic Test', () => {
    test('expects true to be true', () => {
        expect(true).toBe(true);
    });
});